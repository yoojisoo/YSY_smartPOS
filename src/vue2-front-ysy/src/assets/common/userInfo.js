import axios from 'axios'


export default{
    urlBase : "/ysy/v1",
    getUserInfo : ()=>{
        // axios.get(this.urlBase + "/user")
        //then(res=>{
        // store..res
        // })
    }
}